import React from 'react'
import "../../../_styles/components/toggleSidebar.scss"

const ToggleSidebarLayout = props => (
    <div className="ToggleSidebarLayout">
        {props.children}
    </div>
)

export default ToggleSidebarLayout