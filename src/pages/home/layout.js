import React from 'react'
import '../../_styles/Utilities/styles.css'

const HomeLayout = props => (
    <section className="HomeLayout">
        {props.children}
    </section>

)

export default HomeLayout