import React from 'react'
import "../../_styles/components/home.scss"

const HomeLayout = props => (
    <section className="HomeLayout">
        {props.children}
    </section>

)

export default HomeLayout