const data = ( state, action ) => {
    switch ( action.type ) {
        case 'TOOGLE_MENU': {
            return {
                ...state
            }
        }
        default:
            return state

    }
}

export default data